import { View, Text } from 'react-native';

export default function SaferScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Safer Screen</Text>
    </View>
  );
} 